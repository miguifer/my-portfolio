---
interface Tecnologia {
  nombre: string;
  logo: string;
  url: string;
}

interface Props {
  titulo: string;
  descripcion: string;
  repositorio?: string;
  demo?: string | string[];
  imagenes?: string | string[];
  tecnologias?: Tecnologia[];
  enDesarrollo?: boolean;
}

const {
  titulo,
  descripcion,
  repositorio,
  demo,
  imagenes,
  tecnologias = [],
  enDesarrollo = false,
} = Astro.props;
---

<div class="transition-colors">
  <div class="flex flex-col md:flex-row gap-10">
    {
      imagenes && (
        <div class="w-full md:w-1/2 lg:w-3/5">
          <div class="relative w-full h-64 md:h-80 rounded-lg overflow-hidden border border-gray-300 bg-white">
            {Array.isArray(imagenes) ? (
              <div class="carousel relative w-full h-full" data-autoplay="3000">
                <div class="carousel-track flex h-full transition-transform duration-500 ease-in-out">
                  {imagenes.map((img, index) => (
                    <img
                      src={img}
                      alt={`${titulo} demo ${index + 1}`}
                      class="w-full h-full object-cover flex-shrink-0"
                    />
                  ))}
                </div>
                <button type="button" class="carousel-prev absolute left-2 top-1/2 -translate-y-1/2 bg-black/40 hover:bg-black/60 text-white rounded-full w-8 h-8 flex items-center justify-center transition-colors" aria-label="Anterior">
                  <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" /></svg>
                </button>
                <button type="button" class="carousel-next absolute right-2 top-1/2 -translate-y-1/2 bg-black/40 hover:bg-black/60 text-white rounded-full w-8 h-8 flex items-center justify-center transition-colors" aria-label="Siguiente">
                  <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" /></svg>
                </button>
                <div class="carousel-dots absolute bottom-3 left-1/2 -translate-x-1/2 flex gap-2">
                  {imagenes.map((_, index) => (
                    <button type="button" class={`w-2.5 h-2.5 rounded-full transition-colors ${index === 0 ? "bg-white" : "bg-white/50"}`} data-index={index} aria-label={`Ir a imagen ${index + 1}`} />
                  ))}
                </div>
              </div>
            ) : (
              <img
                src={imagenes}
                alt={`${titulo} demo`}
                class="w-full h-full object-cover"
              />
            )}
          </div>
        </div>
      )
    }

    <div
      class={`w-full ${imagenes ? "md:w-1/2 lg:w-2/5" : ""} flex flex-col justify-center`}
    >
      <h3 class="text-2xl font-semibold font-base mb-1">{titulo}</h3>
      {
        enDesarrollo && (
          <span class="inline-block w-fit bg-yellow-200 text-yellow-800 text-xs font-base px-2 py-1 rounded-full mb-2">
            En desarrollo
          </span>
        )
      }
      <p class="text-xl font-base mb-4" set:html={descripcion} />

      {
        tecnologias.length > 0 && (
          <div class="flex flex-wrap gap-3 mb-4">
            {tecnologias.map((tech) => (
              <a
                href={tech.url}
                target="_blank"
                rel="noopener noreferrer"
                title={tech.nombre}
                class="w-8 h-8 hover:scale-110 transition-transform"
              >
                <img
                  src={tech.logo}
                  alt={tech.nombre}
                  class="w-full h-full object-contain"
                />
              </a>
            ))}
          </div>
        )
      }

      <div class="flex gap-3">
        {
          repositorio && (
            <a
              href={repositorio}
              target="_blank"
              rel="noopener noreferrer"
              class="flex font-base items-center gap-1 text-sm text-gray-500 hover:text-red transition-colors"
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                class="w-4 h-4"
                viewBox="0 0 24 24"
                fill="currentColor"
              >
                <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z" />
              </svg>
              Repositorio
            </a>
          )
        }
        {
          demo && (
            <a
              href={demo}
              target="_blank"
              rel="noopener noreferrer"
              class="flex font-base items-center gap-1 text-sm text-gray-500 hover:text-red transition-colors"
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                class="w-4 h-4"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"
                stroke-width="2"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"
                />
              </svg>
              Ver proyecto
            </a>
          )
        }
      </div>
    </div>
  </div>
</div>

<!-- Carrousel js -->
<script>
  document.querySelectorAll<HTMLElement>(".carousel").forEach((carousel) => {
    const track = carousel.querySelector<HTMLElement>(".carousel-track")!;
    const slides = track.children.length;
    const dots = carousel.querySelectorAll<HTMLElement>(".carousel-dots button");
    const autoplayDelay = parseInt(carousel.dataset.autoplay || "3000");
    let current = 0;
    let interval: ReturnType<typeof setInterval>;

    function goTo(index: number) {
      current = ((index % slides) + slides) % slides;
      track.style.transform = `translateX(-${current * 100}%)`;
      dots.forEach((dot, i) => {
        dot.classList.toggle("bg-white", i === current);
        dot.classList.toggle("bg-white/50", i !== current);
      });
    }

    carousel.querySelector(".carousel-prev")!.addEventListener("click", () => {
      goTo(current - 1);
      resetAutoplay();
    });

    carousel.querySelector(".carousel-next")!.addEventListener("click", () => {
      goTo(current + 1);
      resetAutoplay();
    });

    dots.forEach((dot) => {
      dot.addEventListener("click", () => {
        goTo(parseInt(dot.dataset.index || "0"));
        resetAutoplay();
      });
    });

    function resetAutoplay() {
      clearInterval(interval);
      interval = setInterval(() => goTo(current + 1), autoplayDelay);
    }

    // Touch/swipe support
    let touchStartX = 0;
    let touchEndX = 0;

    carousel.addEventListener("touchstart", (e) => {
      touchStartX = e.changedTouches[0].screenX;
      clearInterval(interval);
    }, { passive: true });

    carousel.addEventListener("touchend", (e) => {
      touchEndX = e.changedTouches[0].screenX;
      const diff = touchStartX - touchEndX;
      if (Math.abs(diff) > 50) {
        if (diff > 0) goTo(current + 1);
        else goTo(current - 1);
      }
      resetAutoplay();
    });

    interval = setInterval(() => goTo(current + 1), autoplayDelay);
  });
</script>
